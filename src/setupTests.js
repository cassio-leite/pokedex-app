// src/setupTests.js

// Este arquivo é executado antes de cada teste.
// Importamos o @testing-library/jest-dom para ter acesso aos matchers estendidos
// como .toBeInTheDocument(), .toHaveTextContent(), etc.
import '@testing-library/jest-dom';